node_modules/
dist/
.next/
coverage/
.env
.env.*.local
# Wrangler : variables locales du worker (jamais versionnées, lues en priorité
# par `wrangler dev` — c'est là que vivent localhost et les secrets de dev).
.dev.vars
*.log
.DS_Store
uploads/
*.db
*.db-journal
*.db-shm
*.db-wal
*.tsbuildinfo

# Secrets auto-hébergement (jamais commités)
docker.env
docker.env.bak
cloudflared-data/*.json
cloudflared-data/*.pem
tools/eco-transcoder/eco-transcoder.env

# Service worker généré au build depuis sw.template.js (version unique par déploiement)
apps/web/public/sw.js
.zcode/
